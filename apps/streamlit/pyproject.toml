[project]
name = "programmation-pour-la-finance"
version = "0.0"
description = "Version web d'un demonstrateur pour une forte expostion des statégies d'investissement pour construite un portefeuille"
authors = [
    {name = "Charly-Anasthasia"}
]
license = {text = "Opensource"}
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
